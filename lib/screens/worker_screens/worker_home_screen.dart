import 'package:flutter/material.dart';

class WorkerHomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Worker Home')),
    );
  }
}
